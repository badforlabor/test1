using UnityEngine;
using System.Collections;

public class testq1 : MonoBehaviour {

	// Use this for initialization
	void Start () {
	    Quaternion basePos = transform.rotation;
        Quaternion basePos2 = Quaternion.AngleAxis(30, Vector3.zero);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
